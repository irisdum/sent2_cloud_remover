#Files to deal with the dataset just before its enters into the model

def create_dataset(input_dir,list_id,output_dir):
    """Create a copy of these images of list_id in output_dir (train) to train the model"""
    #Clean creation of output_dir with dataX and Label
    #Copy them
    pass
